version: "2.1"

services:
  db:
    image: postgres:14
    container_name: robot-transactions
    ports:
      - "9001:5432"
    environment:
      - POSTGRES_USER=pguser
      - POSTGRES_PASSWORD=pgpass 
      - POSTGRES_DB=robot-transact